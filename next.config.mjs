/** @type {import('next').NextConfig} */

const nextConfig = {
    output: "export",
    basePath: '/Author_new',
    assetPrefix: '/pages/',
    reactStrictMode: true,
};

export default nextConfig;
